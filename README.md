# Startup_success_prediction
"A machine learning project to predict the likelihood of startup success (Acquisition/IPO) based on funding, industry, and founder metrics."
## How to Run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `python app.py`
